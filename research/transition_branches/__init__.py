"""Independent research workflow for coherent transition branches.

The package deliberately avoids eager imports so that its command-line module
can be executed with ``python -m`` without loading the module twice.
"""
